import 'package:injectable/injectable.dart';

/// Injection module for application-level dependencies
@module
abstract class AppModule {
  // Define external dependencies here, such as third-party services
  // that cannot be annotated directly with @injectable
}
